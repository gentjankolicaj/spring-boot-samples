services:
  web-postgres-db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=web1_db
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: